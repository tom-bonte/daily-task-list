# Static site: no build step, serve public/ as-is.
[build]
  publish = "public"
  command = "echo 'No build step'"

# Always revalidate app code so a new push shows up on the next reload.
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache"
